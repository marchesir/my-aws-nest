config:
# defaults to auto gnerate.
# my-aws-nest:name: my-aws-nest
  my-aws-nest:vpc:
    - cidrBlock: 10.0.0.0/16
      subnets:
        - cidrBlock: 10.0.1.0/24
        - type: private
  aws:region: eu-west-2
  
environment:
  # Depending on the service, you will need Pulumi ESC environment that provides the required IAM roles.
  #- global/esc-aws-s3full
  - global/esc-aws-vpcfull