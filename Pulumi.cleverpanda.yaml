config:
  aws:region: eu-west-2
  type: eks
  name: cleverpanda
  #cidr: 10.0.0.0/16
  #azCount: 3
  #kubernetesVersion: "1.33"
  #instanceType: t3.medium
  #autoScaling: false
  #minSize: 1
  #maxSize: 3
  #desiredCapacity: 2

  environment:
    # Depending on the service, you will need Pulumi ESC environment that provides the required IAM roles.
    - global/esc-aws-eksfull